[build]
  command = "npm run build"
  publish = "dist/public"

# SPA fallback - all routes serve index.html for client-side routing
# Static assets are served first automatically before this rule applies
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
